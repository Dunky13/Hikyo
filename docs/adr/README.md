# Frozen — the ADRs moved to `main`

Since 2026-08-19 (Hikyo-Org/Hikyo#171) the canonical architecture decision
records, specification set and research notes live on `main` under
`docs/adr/`, `docs/spec/` and `docs/research/`. The copies on this branch are
frozen at that date and are not amended. Amend an ADR on `main` through the
procedure in `docs/adr/oss-mechanics.md` § Governance.

This branch remains the home of the frozen UI prototypes under `prototype/`.
