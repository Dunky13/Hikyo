apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hikyo.fullname" . }}-release-trust
  labels:
    {{- include "hikyo.labels" . | nindent 4 }}
data:
  root.json: |
{{ .Files.Get "files/release/trust/root.json" | indent 4 }}
  primary.pub: |
{{ .Files.Get "files/release/trust/primary.pub" | indent 4 }}
