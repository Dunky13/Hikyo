apiVersion: v1
kind: Service
metadata:
  name: {{ include "wenv.fullname" . }}
  labels:
    {{- include "wenv.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "wenv.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
