replicaCount: 1

image:
  repository: ghcr.io/dunky13/wenv
  # Release packaging replaces this value with the signed image index digest.
  digest: sha256:RELEASE_IMAGE_DIGEST

database:
  # Name of an existing Secret whose WENV_DB key contains the production DSN.
  existingSecret: ""

service:
  port: 8080

network:
  # A non-loopback plaintext listener is only valid behind an explicitly named
  # reverse-proxy boundary. Rendering fails while this list is empty.
  trustedProxyCIDRs: []

releaseTrust:
  admissionPolicy:
    # Requires Kyverno. The policy verifies the exact Wenv image repository
    # with the offline primary key packaged into the release chart.
    enabled: false

resources: {}
